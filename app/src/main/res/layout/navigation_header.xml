<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                android:layout_width="match_parent"
                android:layout_height="@dimen/menu_header_height"
                android:background="@raw/background_material"
                android:padding="@dimen/menu_header_padding">

    <com.github.siyamed.shapeimageview.CircularImageView
        android:layout_marginTop="@dimen/menu_header_top_extra_padding"
        android:id="@+id/ivLogo"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:siBorderWidth="1dp"
        app:siBorderColor="@android:color/white"
        android:layout_above="@+id/tvProfileName"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:src="@raw/ic_user_icon_placeholder"/>

    <TextView
        android:id="@+id/tvProfileName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/tvProfileEmail"
        android:layout_marginTop="@dimen/menu_header_text_margin_top"
        android:singleLine="true"
        android:text="My Girl"
        android:textColor="@android:color/white"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/tvProfileEmail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:singleLine="true"
        android:text="email.only.me.known@mail.com"
        android:textColor="@android:color/white"/>

</RelativeLayout>